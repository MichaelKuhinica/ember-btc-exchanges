import { directionalPrice } from '../../../helpers/directional-price';
import { module, test } from 'qunit';

module('Unit | Helper | directional price');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = directionalPrice(42);
  assert.ok(result);
});
